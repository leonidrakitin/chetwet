commit 478f3bdbb7c9bbe1931191cdd32dd570c5ddafc3
Author: Ракитин Леонид Павлович <lrakitin@alfabank.ru>
Date:   Tue Feb 17 19:21:24 2026 +0300

    feat(captain): add Qwen models, separate embedding host/key
    
    - llm.yml: add qwen2.5:0.5b, qwen3-embedding:0.6b; add to editor/assistant/copilot/label_suggestion and help_center_search
    - EmbeddingService: support CAPTAIN_EMBEDDING_API_KEY and CAPTAIN_EMBEDDING_ENDPOINT for separate embedding host
    - Super admin: add CAPTAIN_EMBEDDING_API_KEY, CAPTAIN_EMBEDDING_ENDPOINT to captain config list
    
    Co-authored-by: Cursor <cursoragent@cursor.com>

diff --git a/app/controllers/super_admin/app_configs_controller.rb b/app/controllers/super_admin/app_configs_controller.rb
index 1ff01c0d4..36221dc8f 100644
--- a/app/controllers/super_admin/app_configs_controller.rb
+++ b/app/controllers/super_admin/app_configs_controller.rb
@@ -51,7 +51,7 @@ class SuperAdmin::AppConfigsController < SuperAdmin::ApplicationController
       'notion' => %w[NOTION_CLIENT_ID NOTION_CLIENT_SECRET],
       'google' => %w[GOOGLE_OAUTH_CLIENT_ID GOOGLE_OAUTH_CLIENT_SECRET GOOGLE_OAUTH_REDIRECT_URI ENABLE_GOOGLE_OAUTH_LOGIN],
       'captain' => %w[CAPTAIN_OPEN_AI_API_KEY CAPTAIN_OPEN_AI_MODEL CAPTAIN_OPEN_AI_ENDPOINT CAPTAIN_DEEPSEEK_API_KEY CAPTAIN_DEEPSEEK_ENDPOINT
-                       CAPTAIN_QWEN_API_KEY CAPTAIN_QWEN_ENDPOINT]
+                       CAPTAIN_QWEN_API_KEY CAPTAIN_QWEN_ENDPOINT CAPTAIN_EMBEDDING_MODEL CAPTAIN_EMBEDDING_API_KEY CAPTAIN_EMBEDDING_ENDPOINT]
     }
 
     @allowed_configs = mapping.fetch(
diff --git a/config/llm.yml b/config/llm.yml
index 7fa0cd3aa..4034bbe53 100644
--- a/config/llm.yml
+++ b/config/llm.yml
@@ -83,6 +83,14 @@ models:
     provider: qwen
     display_name: 'Qwen MT Lite'
     credit_multiplier: 1
+  "qwen2.5:0.5b":
+    provider: qwen
+    display_name: 'Qwen 2.5 0.5B'
+    credit_multiplier: 1
+  "qwen3-embedding:0.6b":
+    provider: qwen
+    display_name: 'Qwen3 Embedding 0.6B'
+    credit_multiplier: 1
   whisper-1:
     provider: openai
     display_name: 'Whisper'
@@ -110,6 +118,7 @@ features:
         qwen3-coder-plus,
         qwen3.5-plus,
         qwen-mt-lite,
+        "qwen2.5:0.5b",
       ]
     default: gpt-4.1-mini
   assistant:
@@ -128,6 +137,7 @@ features:
         qwen3-coder-plus,
         qwen3.5-plus,
         qwen-mt-lite,
+        "qwen2.5:0.5b",
       ]
     default: gpt-5.1
   copilot:
@@ -146,15 +156,16 @@ features:
         qwen3-coder-plus,
         qwen3.5-plus,
         qwen-mt-lite,
+        "qwen2.5:0.5b",
       ]
     default: gpt-5.1
   label_suggestion:
     models:
-      [gpt-4.1-nano, gpt-4.1-mini, gpt-5-mini, gemini-3-flash, claude-haiku-4.5, deepseek-chat, deepseek-reasoner, qwen3-coder-plus, qwen3.5-plus, qwen-mt-lite]
+      [gpt-4.1-nano, gpt-4.1-mini, gpt-5-mini, gemini-3-flash, claude-haiku-4.5, deepseek-chat, deepseek-reasoner, qwen3-coder-plus, qwen3.5-plus, qwen-mt-lite, "qwen2.5:0.5b"]
     default: gpt-4.1-nano
   audio_transcription:
     models: [whisper-1]
     default: whisper-1
   help_center_search:
-    models: [text-embedding-3-small]
+    models: [text-embedding-3-small, "qwen3-embedding:0.6b"]
     default: text-embedding-3-small
diff --git a/enterprise/app/controllers/enterprise/super_admin/app_configs_controller.rb b/enterprise/app/controllers/enterprise/super_admin/app_configs_controller.rb
index edcf4980a..d4242f985 100644
--- a/enterprise/app/controllers/enterprise/super_admin/app_configs_controller.rb
+++ b/enterprise/app/controllers/enterprise/super_admin/app_configs_controller.rb
@@ -50,6 +50,8 @@ module Enterprise::SuperAdmin::AppConfigsController
       CAPTAIN_QWEN_API_KEY
       CAPTAIN_QWEN_ENDPOINT
       CAPTAIN_EMBEDDING_MODEL
+      CAPTAIN_EMBEDDING_API_KEY
+      CAPTAIN_EMBEDDING_ENDPOINT
       CAPTAIN_FIRECRAWL_API_KEY
     ]
   end
diff --git a/enterprise/app/services/captain/llm/embedding_service.rb b/enterprise/app/services/captain/llm/embedding_service.rb
index 2fac54594..5c284b449 100644
--- a/enterprise/app/services/captain/llm/embedding_service.rb
+++ b/enterprise/app/services/captain/llm/embedding_service.rb
@@ -17,7 +17,13 @@ class Captain::Llm::EmbeddingService
     return [] if content.blank?
 
     instrument_embedding_call(instrumentation_params(content, model)) do
-      RubyLLM.embed(content, model: model).vectors
+      if embedding_api_key.present? && embedding_api_base.present?
+        Llm::Config.with_api_key(embedding_api_key, api_base: embedding_api_base) do
+          RubyLLM.embed(content, model: model, provider: :openai, assume_model_exists: true).vectors
+        end
+      else
+        RubyLLM.embed(content, model: model).vectors
+      end
     end
   rescue RubyLLM::Error => e
     Rails.logger.error "Embedding API Error: #{e.message}"
@@ -26,6 +32,17 @@ class Captain::Llm::EmbeddingService
 
   private
 
+  def embedding_api_key
+    @embedding_api_key ||= InstallationConfig.find_by(name: 'CAPTAIN_EMBEDDING_API_KEY')&.value
+  end
+
+  def embedding_api_base
+    return @embedding_api_base if defined?(@embedding_api_base)
+
+    endpoint = InstallationConfig.find_by(name: 'CAPTAIN_EMBEDDING_ENDPOINT')&.value.presence
+    @embedding_api_base = endpoint.present? ? "#{endpoint.chomp('/')}/v1" : nil
+  end
+
   def instrumentation_params(content, model)
     {
       span_name: 'llm.captain.embedding',
---
commit 342146e46e8151f9f923f31e1684d464719f3a99
Author: Ракитин Леонид Павлович <lrakitin@alfabank.ru>
Date:   Tue Feb 17 20:48:33 2026 +0300

    feat(captian): improved chat helper

diff --git a/config/llm.yml b/config/llm.yml
index 4034bbe53..4bb34d623 100644
--- a/config/llm.yml
+++ b/config/llm.yml
@@ -83,11 +83,11 @@ models:
     provider: qwen
     display_name: 'Qwen MT Lite'
     credit_multiplier: 1
-  "qwen2.5:0.5b":
+  qwen2.5:0.5b:
     provider: qwen
     display_name: 'Qwen 2.5 0.5B'
     credit_multiplier: 1
-  "qwen3-embedding:0.6b":
+  qwen3-embedding:0.6b:
     provider: qwen
     display_name: 'Qwen3 Embedding 0.6B'
     credit_multiplier: 1
@@ -118,7 +118,7 @@ features:
         qwen3-coder-plus,
         qwen3.5-plus,
         qwen-mt-lite,
-        "qwen2.5:0.5b",
+        qwen2.5:0.5b,
       ]
     default: gpt-4.1-mini
   assistant:
@@ -137,7 +137,7 @@ features:
         qwen3-coder-plus,
         qwen3.5-plus,
         qwen-mt-lite,
-        "qwen2.5:0.5b",
+        qwen2.5:0.5b,
       ]
     default: gpt-5.1
   copilot:
@@ -156,16 +156,16 @@ features:
         qwen3-coder-plus,
         qwen3.5-plus,
         qwen-mt-lite,
-        "qwen2.5:0.5b",
+        qwen2.5:0.5b,
       ]
     default: gpt-5.1
   label_suggestion:
     models:
-      [gpt-4.1-nano, gpt-4.1-mini, gpt-5-mini, gemini-3-flash, claude-haiku-4.5, deepseek-chat, deepseek-reasoner, qwen3-coder-plus, qwen3.5-plus, qwen-mt-lite, "qwen2.5:0.5b"]
+      [gpt-4.1-nano, gpt-4.1-mini, gpt-5-mini, gemini-3-flash, claude-haiku-4.5, deepseek-chat, deepseek-reasoner, qwen3-coder-plus, qwen3.5-plus, qwen-mt-lite, qwen2.5:0.5b]
     default: gpt-4.1-nano
   audio_transcription:
     models: [whisper-1]
     default: whisper-1
   help_center_search:
-    models: [text-embedding-3-small, "qwen3-embedding:0.6b"]
+    models: [text-embedding-3-small, qwen3-embedding:0.6b]
     default: text-embedding-3-small
diff --git a/enterprise/app/services/llm/base_ai_service.rb b/enterprise/app/services/llm/base_ai_service.rb
index a5a91cf24..13be0dbb3 100644
--- a/enterprise/app/services/llm/base_ai_service.rb
+++ b/enterprise/app/services/llm/base_ai_service.rb
@@ -15,7 +15,9 @@ class Llm::BaseAiService
   end
 
   def chat(model: @model, temperature: @temperature)
-    RubyLLM.chat(model: model).with_temperature(temperature)
+    Llm::Config.with_api_key(resolve_api_key(model), api_base: resolve_api_base(model)) do |context|
+      context.chat(model: model, provider: :openai, assume_model_exists: true).with_temperature(temperature)
+    end
   end
 
   private
@@ -28,4 +30,56 @@ class Llm::BaseAiService
   def setup_temperature
     @temperature = DEFAULT_TEMPERATURE
   end
+
+  def api_key
+    @api_key ||= InstallationConfig.find_by(name: 'CAPTAIN_OPEN_AI_API_KEY')&.value
+  end
+
+  def api_base
+    endpoint = InstallationConfig.find_by(name: 'CAPTAIN_OPEN_AI_ENDPOINT')&.value.presence || 'https://api.openai.com/'
+    endpoint = endpoint.chomp('/')
+    "#{endpoint}/v1"
+  end
+
+  def deepseek_model?(model_name)
+    Llm::Models.models.dig(model_name, 'provider') == 'deepseek'
+  end
+
+  def qwen_model?(model_name)
+    Llm::Models.models.dig(model_name, 'provider') == 'qwen'
+  end
+
+  def resolve_api_key(model_name)
+    return deepseek_api_key if deepseek_model?(model_name) && deepseek_api_key.present?
+    return qwen_api_key if qwen_model?(model_name) && qwen_api_key.present?
+
+    api_key
+  end
+
+  def resolve_api_base(model_name)
+    return deepseek_api_base if deepseek_model?(model_name) && deepseek_api_key.present?
+    return qwen_api_base if qwen_model?(model_name) && qwen_api_key.present?
+
+    api_base
+  end
+
+  def deepseek_api_key
+    @deepseek_api_key ||= InstallationConfig.find_by(name: 'CAPTAIN_DEEPSEEK_API_KEY')&.value
+  end
+
+  def deepseek_api_base
+    endpoint = InstallationConfig.find_by(name: 'CAPTAIN_DEEPSEEK_ENDPOINT')&.value.presence || 'https://api.deepseek.com/'
+    endpoint = endpoint.chomp('/')
+    "#{endpoint}/v1"
+  end
+
+  def qwen_api_key
+    @qwen_api_key ||= InstallationConfig.find_by(name: 'CAPTAIN_QWEN_API_KEY')&.value
+  end
+
+  def qwen_api_base
+    endpoint = InstallationConfig.find_by(name: 'CAPTAIN_QWEN_ENDPOINT')&.value.presence || 'https://dashscope.aliyuncs.com/compatible-mode/'
+    endpoint = endpoint.chomp('/')
+    "#{endpoint}/v1"
+  end
 end
---
commit cb17e6b08bce463536b263075778c68fbf903577
Author: Ракитин Леонид Павлович <lrakitin@alfabank.ru>
Date:   Tue Feb 17 22:51:30 2026 +0300

    refactor(captain): centralize multi-provider config in BaseAiService
    
    Replace hardcoded per-provider methods (deepseek_model?, qwen_model?,
    deepseek_api_key, etc.) with a PROVIDER_CONFIGS registry hash.
    Add memoized fetch_config to reduce repeated DB lookups.
    Accept optional account: kwarg for future per-account overrides.
    
    Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>

diff --git a/enterprise/app/services/llm/base_ai_service.rb b/enterprise/app/services/llm/base_ai_service.rb
index 13be0dbb3..46ef28a06 100644
--- a/enterprise/app/services/llm/base_ai_service.rb
+++ b/enterprise/app/services/llm/base_ai_service.rb
@@ -2,14 +2,43 @@
 
 # Base service for LLM operations using RubyLLM.
 # New features should inherit from this class.
+#
+# Supports multiple providers (OpenAI, DeepSeek, Qwen, etc.) with automatic
+# API key and endpoint resolution based on the selected model's provider.
+# Provider configs are resolved from InstallationConfig using a naming convention:
+#   CAPTAIN_{PROVIDER}_API_KEY, CAPTAIN_{PROVIDER}_ENDPOINT
 class Llm::BaseAiService
   DEFAULT_MODEL = Llm::Config::DEFAULT_MODEL
   DEFAULT_TEMPERATURE = 1.0
 
+  # Provider registry: provider name => { key_name:, endpoint_name:, default_endpoint: }
+  # To add a new provider, add an entry here and configure keys in InstallationConfig.
+  PROVIDER_CONFIGS = {
+    'openai' => {
+      key_name: 'CAPTAIN_OPEN_AI_API_KEY',
+      endpoint_name: 'CAPTAIN_OPEN_AI_ENDPOINT',
+      default_endpoint: 'https://api.openai.com/'
+    },
+    'deepseek' => {
+      key_name: 'CAPTAIN_DEEPSEEK_API_KEY',
+      endpoint_name: 'CAPTAIN_DEEPSEEK_ENDPOINT',
+      default_endpoint: 'https://api.deepseek.com/'
+    },
+    'qwen' => {
+      key_name: 'CAPTAIN_QWEN_API_KEY',
+      endpoint_name: 'CAPTAIN_QWEN_ENDPOINT',
+      default_endpoint: 'https://dashscope.aliyuncs.com/compatible-mode/'
+    }
+  }.freeze
+
+  # Default provider used when a model's provider has no dedicated config
+  DEFAULT_PROVIDER = 'openai'
+
   attr_reader :model, :temperature
 
-  def initialize
+  def initialize(account: nil)
     Llm::Config.initialize!
+    @account = account
     setup_model
     setup_temperature
   end
@@ -23,63 +52,66 @@ class Llm::BaseAiService
   private
 
   def setup_model
-    config_value = InstallationConfig.find_by(name: 'CAPTAIN_OPEN_AI_MODEL')&.value
-    @model = (config_value.presence || DEFAULT_MODEL)
+    config_value = fetch_config('CAPTAIN_OPEN_AI_MODEL')
+    @model = config_value.presence || DEFAULT_MODEL
   end
 
   def setup_temperature
     @temperature = DEFAULT_TEMPERATURE
   end
 
-  def api_key
-    @api_key ||= InstallationConfig.find_by(name: 'CAPTAIN_OPEN_AI_API_KEY')&.value
-  end
-
-  def api_base
-    endpoint = InstallationConfig.find_by(name: 'CAPTAIN_OPEN_AI_ENDPOINT')&.value.presence || 'https://api.openai.com/'
-    endpoint = endpoint.chomp('/')
-    "#{endpoint}/v1"
-  end
-
-  def deepseek_model?(model_name)
-    Llm::Models.models.dig(model_name, 'provider') == 'deepseek'
+  # Resolves provider name for a given model from llm.yml config.
+  # Falls back to DEFAULT_PROVIDER if the model is not found.
+  def provider_for(model_name)
+    Llm::Models.models.dig(model_name, 'provider') || DEFAULT_PROVIDER
   end
 
-  def qwen_model?(model_name)
-    Llm::Models.models.dig(model_name, 'provider') == 'qwen'
+  # Returns the provider config hash, falling back to openai for unknown providers.
+  def provider_config_for(model_name)
+    provider = provider_for(model_name)
+    PROVIDER_CONFIGS[provider] || PROVIDER_CONFIGS[DEFAULT_PROVIDER]
   end
 
+  # Resolves the API key for a model. Tries the model's provider first,
+  # then falls back to the default (OpenAI) provider key.
   def resolve_api_key(model_name)
-    return deepseek_api_key if deepseek_model?(model_name) && deepseek_api_key.present?
-    return qwen_api_key if qwen_model?(model_name) && qwen_api_key.present?
+    config = provider_config_for(model_name)
+    key = fetch_config(config[:key_name])
+    return key if key.present?
+
+    # Fall back to default provider key if the model's provider has no key configured
+    return fetch_config(PROVIDER_CONFIGS[DEFAULT_PROVIDER][:key_name]) if config != PROVIDER_CONFIGS[DEFAULT_PROVIDER]
 
-    api_key
+    key
   end
 
+  # Resolves the API base URL for a model. Uses the model's provider endpoint
+  # if its API key is present, otherwise falls back to the default provider.
   def resolve_api_base(model_name)
-    return deepseek_api_base if deepseek_model?(model_name) && deepseek_api_key.present?
-    return qwen_api_base if qwen_model?(model_name) && qwen_api_key.present?
+    config = provider_config_for(model_name)
+    key = fetch_config(config[:key_name])
 
-    api_base
-  end
+    active_config = if key.present?
+                      config
+                    else
+                      PROVIDER_CONFIGS[DEFAULT_PROVIDER]
+                    end
 
-  def deepseek_api_key
-    @deepseek_api_key ||= InstallationConfig.find_by(name: 'CAPTAIN_DEEPSEEK_API_KEY')&.value
+    build_api_base(active_config)
   end
 
-  def deepseek_api_base
-    endpoint = InstallationConfig.find_by(name: 'CAPTAIN_DEEPSEEK_ENDPOINT')&.value.presence || 'https://api.deepseek.com/'
+  def build_api_base(config)
+    endpoint = fetch_config(config[:endpoint_name]).presence || config[:default_endpoint]
     endpoint = endpoint.chomp('/')
     "#{endpoint}/v1"
   end
 
-  def qwen_api_key
-    @qwen_api_key ||= InstallationConfig.find_by(name: 'CAPTAIN_QWEN_API_KEY')&.value
-  end
+  # Fetches a config value from InstallationConfig with memoization.
+  # Uses instance-level cache to avoid repeated DB lookups within a single service call.
+  def fetch_config(name)
+    @config_cache ||= {}
+    return @config_cache[name] if @config_cache.key?(name)
 
-  def qwen_api_base
-    endpoint = InstallationConfig.find_by(name: 'CAPTAIN_QWEN_ENDPOINT')&.value.presence || 'https://dashscope.aliyuncs.com/compatible-mode/'
-    endpoint = endpoint.chomp('/')
-    "#{endpoint}/v1"
+    @config_cache[name] = InstallationConfig.find_by(name: name)&.value
   end
 end
---
commit da2a7e46f7bc25f0807526433bff648d8b65b434
Author: Ракитин Леонид Павлович <lrakitin@alfabank.ru>
Date:   Tue Feb 17 23:39:05 2026 +0300

    refactor(captain): centralize provider configs in Llm::Config
    
    Move PROVIDER_CONFIGS registry to Llm::Config as single source of truth.
    Replace per-provider methods in configure_ruby_llm with a generic loop.
    BaseAiService now references Llm::Config::PROVIDER_CONFIGS directly.
    
    Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>

diff --git a/enterprise/app/services/llm/base_ai_service.rb b/enterprise/app/services/llm/base_ai_service.rb
index 46ef28a06..73959ab12 100644
--- a/enterprise/app/services/llm/base_ai_service.rb
+++ b/enterprise/app/services/llm/base_ai_service.rb
@@ -11,25 +11,9 @@ class Llm::BaseAiService
   DEFAULT_MODEL = Llm::Config::DEFAULT_MODEL
   DEFAULT_TEMPERATURE = 1.0
 
-  # Provider registry: provider name => { key_name:, endpoint_name:, default_endpoint: }
-  # To add a new provider, add an entry here and configure keys in InstallationConfig.
-  PROVIDER_CONFIGS = {
-    'openai' => {
-      key_name: 'CAPTAIN_OPEN_AI_API_KEY',
-      endpoint_name: 'CAPTAIN_OPEN_AI_ENDPOINT',
-      default_endpoint: 'https://api.openai.com/'
-    },
-    'deepseek' => {
-      key_name: 'CAPTAIN_DEEPSEEK_API_KEY',
-      endpoint_name: 'CAPTAIN_DEEPSEEK_ENDPOINT',
-      default_endpoint: 'https://api.deepseek.com/'
-    },
-    'qwen' => {
-      key_name: 'CAPTAIN_QWEN_API_KEY',
-      endpoint_name: 'CAPTAIN_QWEN_ENDPOINT',
-      default_endpoint: 'https://dashscope.aliyuncs.com/compatible-mode/'
-    }
-  }.freeze
+  # Provider registry defined in Llm::Config::PROVIDER_CONFIGS.
+  # To add a new provider, add an entry there and configure keys in InstallationConfig.
+  PROVIDER_CONFIGS = Llm::Config::PROVIDER_CONFIGS
 
   # Default provider used when a model's provider has no dedicated config
   DEFAULT_PROVIDER = 'openai'
diff --git a/lib/llm/config.rb b/lib/llm/config.rb
index 546b6bd4f..b7aeec324 100644
--- a/lib/llm/config.rb
+++ b/lib/llm/config.rb
@@ -1,8 +1,35 @@
 require 'ruby_llm'
 
+# Global RubyLLM configuration and per-request context management.
+#
+# Configures RubyLLM once on first use with system-level API keys from InstallationConfig.
+# Provides `with_api_key` for per-request overrides (used by BaseAiService and ChatHelper).
 module Llm::Config
   DEFAULT_MODEL = 'gpt-4.1-mini'.freeze
 
+  # Provider configs shared with BaseAiService to avoid duplication.
+  # Maps RubyLLM config attribute prefixes to InstallationConfig key names.
+  PROVIDER_CONFIGS = {
+    'openai' => {
+      key_name: 'CAPTAIN_OPEN_AI_API_KEY',
+      endpoint_name: 'CAPTAIN_OPEN_AI_ENDPOINT',
+      default_endpoint: 'https://api.openai.com/',
+      ruby_llm_prefix: 'openai'
+    },
+    'deepseek' => {
+      key_name: 'CAPTAIN_DEEPSEEK_API_KEY',
+      endpoint_name: 'CAPTAIN_DEEPSEEK_ENDPOINT',
+      default_endpoint: 'https://api.deepseek.com/',
+      ruby_llm_prefix: 'deepseek'
+    },
+    'qwen' => {
+      key_name: 'CAPTAIN_QWEN_API_KEY',
+      endpoint_name: 'CAPTAIN_QWEN_ENDPOINT',
+      default_endpoint: 'https://dashscope.aliyuncs.com/compatible-mode/',
+      ruby_llm_prefix: 'qwen'
+    }
+  }.freeze
+
   class << self
     def initialized?
       @initialized ||= false
@@ -33,19 +60,26 @@ module Llm::Config
 
     def configure_ruby_llm
       RubyLLM.configure do |config|
-        config.openai_api_key = system_api_key if system_api_key.present?
-        config.openai_api_base = openai_endpoint.chomp('/') if openai_endpoint.present?
         config.openai_use_system_role = true
         config.logger = Rails.logger
+
+        PROVIDER_CONFIGS.each_value { |provider| configure_provider(config, provider) }
       end
     end
 
-    def system_api_key
-      InstallationConfig.find_by(name: 'CAPTAIN_OPEN_AI_API_KEY')&.value
+    def configure_provider(config, provider)
+      api_key = fetch_config(provider[:key_name])
+      return if api_key.blank?
+
+      prefix = provider[:ruby_llm_prefix]
+      config.public_send(:"#{prefix}_api_key=", api_key)
+
+      endpoint = fetch_config(provider[:endpoint_name]).presence || provider[:default_endpoint]
+      config.public_send(:"#{prefix}_api_base=", endpoint.chomp('/'))
     end
 
-    def openai_endpoint
-      InstallationConfig.find_by(name: 'CAPTAIN_OPEN_AI_ENDPOINT')&.value
+    def fetch_config(name)
+      InstallationConfig.find_by(name: name)&.value
     end
   end
 end
---
commit 68adcd3f3336fbbb9a440b32a9c307b1d052fa0d
Author: Ракитин Леонид Павлович <lrakitin@alfabank.ru>
Date:   Wed Feb 18 00:18:50 2026 +0300

    feat(captian): updated config.rb

diff --git a/lib/llm/config.rb b/lib/llm/config.rb
index b7aeec324..d109a297d 100644
--- a/lib/llm/config.rb
+++ b/lib/llm/config.rb
@@ -74,8 +74,12 @@ module Llm::Config
       prefix = provider[:ruby_llm_prefix]
       config.public_send(:"#{prefix}_api_key=", api_key)
 
+      # Only set api_base when the gem supports it (e.g. openai does, deepseek/qwen may not)
+      base_setter = :"#{prefix}_api_base="
+      return unless config.respond_to?(base_setter)
+
       endpoint = fetch_config(provider[:endpoint_name]).presence || provider[:default_endpoint]
-      config.public_send(:"#{prefix}_api_base=", endpoint.chomp('/'))
+      config.public_send(base_setter, endpoint.chomp('/'))
     end
 
     def fetch_config(name)
---
commit fb1eeac4706b98c4a5a9754e6433964b2d1321ab
Author: Ракитин Леонид Павлович <lrakitin@alfabank.ru>
Date:   Wed Feb 18 00:29:13 2026 +0300

    feat(captian): updated config.rb

diff --git a/lib/llm/config.rb b/lib/llm/config.rb
index d109a297d..b2873c9ce 100644
--- a/lib/llm/config.rb
+++ b/lib/llm/config.rb
@@ -72,7 +72,10 @@ module Llm::Config
       return if api_key.blank?
 
       prefix = provider[:ruby_llm_prefix]
-      config.public_send(:"#{prefix}_api_key=", api_key)
+      key_setter = :"#{prefix}_api_key="
+      return unless config.respond_to?(key_setter)
+
+      config.public_send(key_setter, api_key)
 
       # Only set api_base when the gem supports it (e.g. openai does, deepseek/qwen may not)
       base_setter = :"#{prefix}_api_base="
---
commit 8450d6b8d84b68b36759b0a69f355ae83aad670c
Author: Ракитин Леонид Павлович <lrakitin@alfabank.ru>
Date:   Wed Feb 18 00:43:37 2026 +0300

    feat(captian): updated chat_helper.rb

diff --git a/enterprise/app/helpers/captain/chat_helper.rb b/enterprise/app/helpers/captain/chat_helper.rb
index 7d5d297b8..a5d7b1364 100644
--- a/enterprise/app/helpers/captain/chat_helper.rb
+++ b/enterprise/app/helpers/captain/chat_helper.rb
@@ -90,8 +90,9 @@ module Captain::ChatHelper
 
   def build_chat(context)
     llm_chat = context.chat(model: @model, provider: :openai, assume_model_exists: true).with_temperature(temperature)
-    llm_chat = llm_chat.with_params(response_format: { type: 'json_object' })
-
+    unless deepseek_model?(@model) || qwen_model?(@model)
+      llm_chat = llm_chat.with_params(response_format: { type: 'json_object' })
+    end
     llm_chat = setup_tools(llm_chat)
     llm_chat = setup_system_instructions(llm_chat)
     setup_event_handlers(llm_chat)
---
